inventory:
  size: 3
storage:
  pokemonsPerPage: 24
leveling:
  # In seonds
  experienceGainCooldow: 10
  # Formula: level variabble can be used
  experiencePerMessage: "60"
  # Formula: level variabble can be used
  experiencePerLevel: level * 100
  pointsAbilityPerLevel:
    min: 1
    max: 5
